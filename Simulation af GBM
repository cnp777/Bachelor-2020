library(sde)

# Parametre
S0 <- 100     # Startværdi
mu <- 0.05    # Drift
sigma <- 0.2  # Volatilitet

# Time grid
T <- 1         # Sluttid
n <- 1000      # Antal evalueringer
dt <- T/ (n)   # Lige lange tidsskridt

# Simulation 
x <- GBM(x=S0, r=mu, sigma=sigma, T=1, N=n) 
plot(x) 
